# Dataset toolkits

These commands build deterministic vocabularies, episode/window indices and
text caches under `derived/`. They never write model-specific windows back into
the public continuous release.
