# Production data adapters

Adapters read continuous TextAgent episodes without modifying the public data.
Model-specific spatial and temporal windows are derived here. The retired M4
feature-sidecar adapter is isolated under `ablations/m4_sidecar/`.
