# Prepared metadata

These files are model-specific views of the external continuous dataset. M2,
M3 and M4 paths are relative to `--dataset-root`, so they remain valid after
the bundle moves. Regeneration commands are in `docs/data.md`.
